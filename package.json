{
  "name": "all-in-one-water-manager",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "dotenv": "^17.2.2",
      "zod": "^4.1.13",
      "typescript": "^5"
    }
  },
  "type": "module",
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "check-types": "bun run --filter '*' check-types",
    "dev:native": "bun run --filter native dev",
    "dev:web": "bun run --filter web dev"
  },
  "dependencies": {
    "@all-in-one-water-manager/env": "workspace:*",
    "dotenv": "catalog:",
    "groq-sdk": "^0.37.0",
    "react-markdown": "^10.1.0",
    "recharts": "^3.7.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@all-in-one-water-manager/config": "workspace:*",
    "@types/node": "^22.13.14",
    "typescript": "catalog:"
  },
  "packageManager": "bun@1.3.6"
}
